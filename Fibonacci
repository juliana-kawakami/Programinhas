#include <stdio.h>

int main()
{
  int termos, num1, num2, num3, i;
  num1=1;
  num2=1;
  i=1;
  printf("Digite um número: ");
  scanf("%d",&termos);
  while (i<=termos){
      printf("%d,", num1);
      num3=num1+num2;
      num1=num2;
      num2=num3;
      i++;
  }

    return 0;
}
